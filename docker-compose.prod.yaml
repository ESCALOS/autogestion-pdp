services:
    app:
        build:
            context: .
            dockerfile: docker/php/Dockerfile
        container_name: app-prod
        restart: always
        volumes:
            - /home/bsv.devel/private:/var/www/html/storage/app/private
        networks:
            - internal
        depends_on:
            - pgsql
            - redis

    pgsql:
        image: 'postgres:17-alpine'
        container_name: pgsql-prod
        restart: always
        environment:
            PGPASSWORD: '${DB_PASSWORD:-secret}'
            POSTGRES_DB: '${DB_DATABASE}'
            POSTGRES_USER: '${DB_USERNAME}'
            POSTGRES_PASSWORD: '${DB_PASSWORD:-secret}'
        volumes:
            - 'autogestion-pgsql:/var/lib/postgresql/data'
        networks:
            - internal
        healthcheck:
            test:
                - CMD
                - pg_isready
                - '-q'
                - '-d'
                - '${DB_DATABASE}'
                - '-U'
                - '${DB_USERNAME}'
            retries: 5
            timeout: 5s

    nginx:
        image: nginx:stable
        container_name: nginx-prod
        restart: always
        ports:
            - '8090:8090'
        volumes:
            - ./:/var/www/html
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
            - ./docker/nginx/ssl:/etc/nginx/ssl
        depends_on:
            - app
        networks:
            - internal

    redis:
        image: 'redis:alpine'
        volumes:
            - 'autogestion-redis:/data'
        networks:
            - internal
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s

networks:
    internal:
        driver: bridge

volumes:
    autogestion-pgsql:
        driver: local
    autogestion-redis:
        driver: local
