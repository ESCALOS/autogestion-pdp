# Servidor HTTP provisional mientras no haya HTTPS
server {
    listen 8090;
    server_name autogestion.pdparacas.com.pe;

    root /var/www/html/public;
    index index.php index.html;

    # Laravel routing
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    client_max_body_size 50M;
}

# ======================================
# BLOQUE HTTPS (solo cuando habiliten 80)
# ======================================

# server {
#     listen 80;
#     server_name autogestion.pdparacas.com.pe;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# server {
#     listen 443 ssl;
#     server_name autogestion.pdparacas.com.pe;

#     root /var/www/html/public;
#     index index.php index.html;

#     ssl_certificate /etc/letsencrypt/live/autogestion.pdparacas.com.pe/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/autogestion.pdparacas.com.pe/privkey.pem;

#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;

#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     location ~ \.php$ {
#         fastcgi_pass app:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
# }
